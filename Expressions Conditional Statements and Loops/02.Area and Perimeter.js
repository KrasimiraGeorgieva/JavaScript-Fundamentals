"use strict";

function areaPerimeter(a,b) {
    let area = a * b;
    let perimeter = (2 * a) + (2 * b);

    console.log(area);
    console.log(perimeter);
}

console.log(areaPerimeter(2.5, 3.14));